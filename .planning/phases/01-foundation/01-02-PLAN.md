---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - what_to_eat_today_web/backend/main.py
  - what_to_eat_today_web/backend/.env
  - what_to_eat_today_web/backend/.env.example
  - what_to_eat_today_web/backend/requirements.txt
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04]
must_haves:
  truths:
    - "uvicorn what_to_eat_today_web.backend.main:app --port 8000 starts without error"
    - "CORS middleware allows http://localhost:5173 origin"
    - ".env loaded via python-dotenv; MIMO_API_KEY accessible"
    - "lifespan creates tables, seeds data, initializes httpx.AsyncClient"
    - "GET /docs returns Swagger UI (200)"
  artifacts:
    - path: "what_to_eat_today_web/backend/main.py"
      provides: "FastAPI app with lifespan + CORS"
    - path: "what_to_eat_today_web/backend/.env.example"
      provides: "Environment variable template"
    - path: "what_to_eat_today_web/backend/requirements.txt"
      provides: "Pinned Python dependencies"
  key_links:
    - from: "main.py"
      to: "database.py"
      via: "import engine, Base for create_all"
    - from: "main.py"
      to: "seed.py"
      via: "import seed_data for lifespan startup"
    - from: "main.py"
      to: ".env"
      via: "load_dotenv() at module level"
---

# Plan 01-02: FastAPI App Infrastructure - main.py + .env + requirements.txt

<objective>
Wire up the FastAPI application entry point with lifespan (DB init + seed + httpx client), CORS middleware, dotenv loading, and dependency manifest. After this plan, the backend starts on port 8000 and serves Swagger docs.

Purpose: This is the runnable skeleton - the backend can boot and prove infrastructure is working end-to-end.
Output: main.py (app entry), .env/.env.example, requirements.txt
</objective>

<execution_context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@what_to_eat_today_web/backend/database.py
@what_to_eat_today_web/backend/models.py
@what_to_eat_today_web/backend/schemas.py
@what_to_eat_today_web/backend/seed.py
</context>

<tasks>

<task id="B1" type="auto">
<name>Task 1: main.py - FastAPI app with lifespan and CORS</name>
<files>what_to_eat_today_web/backend/main.py</files>
<read_first>
- what_to_eat_today_web/backend/database.py
- what_to_eat_today_web/backend/seed.py
- what_to_eat_today_web/backend/main.py
</read_first>
<action>
Rewrite main.py (currently empty stub) with full FastAPI infrastructure (per D-03, D-05 to D-07, D-15, INFRA-01 to INFRA-04):

Top-level imports and dotenv loading:
- from dotenv import load_dotenv; load_dotenv() (per INFRA-02)
- from contextlib import asynccontextmanager
- from fastapi import FastAPI
- from fastapi.middleware.cors import CORSMiddleware
- import httpx

Lifespan (per INFRA-04, D-15):
- @asynccontextmanager async def lifespan(app: FastAPI):
  - Startup: from database import engine, Base; from seed import seed_data
  - Base.metadata.create_all(bind=engine)
  - seed_data()
  - app.state.http_client = httpx.AsyncClient(timeout=30.0) (per D-15)
  - print("Application startup complete")
  - yield
  - Shutdown: await app.state.http_client.aclose()
  - print("Application shutdown complete")

App instance:
- app = FastAPI(title="今天吃什么 API", lifespan=lifespan)

CORS middleware (per INFRA-01, D-05, D-06):
- app.add_middleware(CORSMiddleware, allow_origins=["http://localhost:5173"], allow_credentials=True, allow_methods=["*"], allow_headers=["*"])

No routes in this phase (per D-04). A placeholder health check at GET / returning {"status": "ok"} is acceptable to verify Swagger works.
</action>
<verify>
<automated>cd what_to_eat_today_web/backend && python -c "from main import app; print(app.title); assert app.title == '今天吃什么 API'; print('main.py OK')"</automated>
</verify>
<acceptance_criteria>
- main.py contains from dotenv import load_dotenv followed by load_dotenv()
- main.py contains @asynccontextmanager async def lifespan(app: FastAPI)
- main.py lifespan calls Base.metadata.create_all(bind=engine) and seed_data()
- main.py lifespan sets app.state.http_client = httpx.AsyncClient(timeout=30.0)
- main.py contains app.add_middleware(CORSMiddleware, allow_origins=["http://localhost:5173"])
- main.py app = FastAPI(title="今天吃什么 API", lifespan=lifespan)
- main.py does NOT define any /api/* routes (per D-04)
</acceptance_criteria>
<done>main.py creates FastAPI app with lifespan (DB init + seed + httpx) and CORS for localhost:5173</done>
</task>

<task id="B2" type="auto">
<name>Task 2: .env + .env.example + requirements.txt</name>
<files>what_to_eat_today_web/backend/.env, what_to_eat_today_web/backend/.env.example, what_to_eat_today_web/backend/requirements.txt</files>
<read_first>
- .gitignore
- what_to_eat_today_web/backend/main.py
</read_first>
<action>
Create .env (per INFRA-02):
- MIMO_API_KEY=your-api-key-here (placeholder; real key added by developer)

Create .env.example (template for others):
- MIMO_API_KEY=your-mimo-api-key-here

Create requirements.txt (per RESEARCH.md Standard Stack pinned versions):
- fastapi==0.136.3
- sqlalchemy==2.0.50
- uvicorn==0.49.0
- python-dotenv==1.2.2
- httpx==0.28.1

Verify .gitignore already excludes .env (it does - confirmed in existing .gitignore).
</action>
<verify>
<automated>cd what_to_eat_today_web/backend && python -c "from dotenv import load_dotenv; load_dotenv(); import os; print('MIMO_API_KEY present:', 'MIMO_API_KEY' in os.environ)" && cat requirements.txt | head -5</automated>
</verify>
<acceptance_criteria>
- .env file exists at what_to_eat_today_web/backend/.env with MIMO_API_KEY key
- .env.example file exists with MIMO_API_KEY=your-mimo-api-key-here
- requirements.txt contains exactly: fastapi==0.136.3, sqlalchemy==2.0.50, uvicorn==0.49.0, python-dotenv==1.2.2, httpx==0.28.1
- .gitignore at project root already contains .env rule (no modification needed)
</acceptance_criteria>
<done>.env template created; requirements.txt has 5 pinned dependencies matching RESEARCH.md versions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file | Contains MIMO_API_KEY secret; must not be committed to git |
| CORS | Only http://localhost:5173 allowed; prevents unauthorized origins |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | .env API key | mitigate | .gitignore excludes .env; .env.example has placeholder only |
| T-01-04 | Spoofing | CORS misconfiguration | mitigate | Strict allow_origins=["http://localhost:5173"] per D-06 |
</threat_model>

<verification>
## Verification Commands
- command: cd what_to_eat_today_web/backend && uvicorn main:app --port 8000 --timeout-keep-alive 2 &; sleep 3; curl -s http://localhost:8000/docs | head -5; kill %1 2>/dev/null
  expected: HTML containing "swagger" or "openapi"
- command: cd what_to_eat_today_web/backend && python -c "from main import app; print(app.title)"
  expected: 今天吃什么 API
- command: cat what_to_eat_today_web/backend/requirements.txt
  expected: 5 lines with pinned versions
</verification>

<success_criteria>
- uvicorn main:app --port 8000 starts without error, prints "Application startup complete"
- canteen.db file generated in backend/ directory after startup
- curl http://localhost:8000/docs returns 200 with Swagger UI
- CORS headers present for http://localhost:5173 origin
- .env loaded; MIMO_API_KEY accessible via os.environ
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-02-SUMMARY.md when done
</output>
