---
phase: 02-core-endpoints
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - what_to_eat_today_web/backend/schemas.py
  - what_to_eat_today_web/backend/routes/__init__.py
  - what_to_eat_today_web/backend/routes/canteens.py
  - what_to_eat_today_web/backend/routes/dishes.py
  - what_to_eat_today_web/backend/routes/suggestion.py
  - what_to_eat_today_web/backend/routes/search.py
  - what_to_eat_today_web/backend/main.py
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05, API-06]

must_haves:
  truths:
    - "GET /api/canteens returns JSON array with 3 canteens, each has camelCase keys (openTime)"
    - "GET /api/dishes/recommended returns all 7 dishes with tags as string arrays"
    - "GET /api/dishes/search?keyword=面 returns matching dishes; empty keyword returns []"
    - "GET /api/suggestion/today returns {text, highlightDish} with full nested Dish object"
    - "GET /api/search/hot-keywords returns array of up to 10 tag strings sorted by frequency"
    - "GET /api/search/suggestions?keyword=面 returns max 8 deduplicated matching strings"
  artifacts:
    - path: "what_to_eat_today_web/backend/schemas.py"
      provides: "TodaySuggestionOut schema with text + highlightDish"
      contains: "class TodaySuggestionOut"
    - path: "what_to_eat_today_web/backend/routes/canteens.py"
      provides: "GET /api/canteens endpoint"
      exports: ["router"]
    - path: "what_to_eat_today_web/backend/routes/dishes.py"
      provides: "GET /api/dishes/recommended and GET /api/dishes/search"
      exports: ["router"]
    - path: "what_to_eat_today_web/backend/routes/suggestion.py"
      provides: "GET /api/suggestion/today endpoint"
      exports: ["router"]
    - path: "what_to_eat_today_web/backend/routes/search.py"
      provides: "GET /api/search/hot-keywords and GET /api/search/suggestions"
      exports: ["router"]
    - path: "what_to_eat_today_web/backend/main.py"
      provides: "Router includes for all 4 route modules"
      contains: "include_router"
  key_links:
    - from: "routes/canteens.py"
      to: "database.SessionLocal"
      via: "dependency injection or direct session usage"
      pattern: "SessionLocal"
    - from: "routes/suggestion.py"
      to: "schemas.TodaySuggestionOut"
      via: "response_model"
      pattern: "TodaySuggestionOut"
    - from: "main.py"
      to: "routes/*.py"
      via: "app.include_router"
      pattern: "include_router"
---

## Phase Goal

**As a** NJU Gulou campus student, **I want to** get real canteen and dish data from API endpoints, **so that** the app shows accurate dining info instead of hardcoded mock data.

<objective>
Implement all 6 data API endpoints (canteens, recommended dishes, dish search, today suggestion, hot keywords, search suggestions) with full camelCase response format matching frontend TypeScript types. Covers D-01 through D-10.

Purpose: Deliver the complete data read API so the frontend can switch from mock data to real backend responses.
Output: 4 route files, TodaySuggestionOut schema, main.py router wiring — all 6 endpoints respond to curl with correct JSON shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@what_to_eat_today_web/backend/schemas.py
@what_to_eat_today_web/backend/database.py
@what_to_eat_today_web/backend/models.py
@what_to_eat_today_web/backend/main.py
@what_to_eat_today_web/frontend/src/mock/mockApi.ts
@what_to_eat_today_web/frontend/src/types.ts
@.planning/phases/02-core-endpoints/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TodaySuggestionOut schema + routes package + canteens route</name>
  <files>what_to_eat_today_web/backend/schemas.py, what_to_eat_today_web/backend/routes/__init__.py, what_to_eat_today_web/backend/routes/canteens.py</files>
  <read_first>
    - what_to_eat_today_web/backend/schemas.py (current DishOut and CanteenOut definitions)
    - what_to_eat_today_web/backend/database.py (SessionLocal factory)
    - what_to_eat_today_web/backend/models.py (Canteen ORM model)
    - what_to_eat_today_web/frontend/src/types.ts (TodaySuggestion interface: {text: string, highlightDish?: Dish})
  </read_first>
  <action>
    1. In schemas.py, add class TodaySuggestionOut(BaseModel) per D-03:
       - Fields: text (str), highlight_dish (DishOut)
       - Same ConfigDict as siblings: alias_generator=to_camel, populate_by_name=True, from_attributes=True
       - The alias for highlight_dish will auto-generate to "highlightDish" via to_camel

    2. Create directory what_to_eat_today_web/backend/routes/ with empty __init__.py

    3. Create routes/canteens.py per D-07, D-08:
       - Import APIRouter from fastapi
       - Import SessionLocal from database, Canteen from models, CanteenOut from schemas
       - router = APIRouter(prefix="/api", tags=["canteens"])
       - GET /canteens endpoint: open SessionLocal(), query all Canteen rows, return list
       - response_model=list[CanteenOut]
       - Use try/finally pattern to close session (or context manager with SessionLocal() as session)
  </action>
  <verify>
    <automated>cd what_to_eat_today_web/backend && python -c "from schemas import TodaySuggestionOut; m = TodaySuggestionOut.model_json_schema(); assert 'highlightDish' in str(m) or 'highlight_dish' in str(m); print('TodaySuggestionOut OK')" && python -c "from routes.canteens import router; print('canteens router OK')"</automated>
  </verify>
  <acceptance_criteria>
    - schemas.py contains class TodaySuggestionOut with fields text: str and highlight_dish: DishOut
    - TodaySuggestionOut.model_json_schema() produces alias "highlightDish" for the highlight_dish field
    - routes/__init__.py exists (can be empty)
    - routes/canteens.py exports a variable named router (APIRouter instance)
    - router has a GET route registered at path "/canteens"
  </acceptance_criteria>
  <done>TodaySuggestionOut schema importable with correct camelCase alias; canteens router importable with GET /canteens route registered</done>
</task>

<task type="auto">
  <name>Task 2: Dishes route (recommended + search) and suggestion route</name>
  <files>what_to_eat_today_web/backend/routes/dishes.py, what_to_eat_today_web/backend/routes/suggestion.py</files>
  <read_first>
    - what_to_eat_today_web/backend/models.py (Dish ORM model — columns: name, canteen, window, tags)
    - what_to_eat_today_web/backend/schemas.py (DishOut, TodaySuggestionOut after Task 1)
    - what_to_eat_today_web/frontend/src/mock/mockApi.ts (searchDishes logic lines 142-153, getTodaySuggestion lines 124-136)
    - .planning/phases/02-core-endpoints/02-CONTEXT.md (D-01 through D-06)
  </read_first>
  <action>
    1. Create routes/dishes.py per D-07:
       - router = APIRouter(prefix="/api/dishes", tags=["dishes"])
       - GET /recommended: query all Dish rows, return as list[DishOut] (per STATE.md "return all 7")
       - GET /search with Query parameter keyword (str, default=""):
         Per D-04: SQL LIKE '%keyword%' on 4 columns (name, canteen, window, tags)
         Per D-06: if keyword is empty string or whitespace-only, return empty list []
         Per D-05: no ordering, return all matches
         Build SQLAlchemy or_ filter with Dish.name.contains(keyword), Dish.canteen.contains(keyword), Dish.window.contains(keyword), Dish.tags.contains(keyword)
         response_model=list[DishOut]

    2. Create routes/suggestion.py per D-07:
       - router = APIRouter(prefix="/api/suggestion", tags=["suggestion"])
       - GET /today: response_model=TodaySuggestionOut
       - Per D-01: hardcode 5 suggestion dicts with text + dish_id (mapping from mockApi.ts):
         {"text": "今天二食堂比较拥挤，建议去一食堂川菜窗口试试宫保鸡丁。", "dish_id": "d1"}
         {"text": "天气转凉，推荐来一碗二食堂的番茄牛腩面，暖胃又好吃！", "dish_id": "d2"}
         {"text": "三食堂今天很空闲，想吃清淡的话可以试试轻食沙拉。", "dish_id": "d5"}
         {"text": "想吃辣的？一食堂麻辣烫窗口的麻辣香锅值得一试！", "dish_id": "d3"}
         {"text": "赶时间的话，三食堂快餐窗口的鸡蛋炒饭出餐很快。", "dish_id": "d4"}
       - Per D-02: random.choice one suggestion, query Dish by dish_id from database, construct TodaySuggestionOut(text=..., highlight_dish=dish_obj)
       - Import random at module level
  </action>
  <verify>
    <automated>cd what_to_eat_today_web/backend && python -c "from routes.dishes import router; routes = [r.path for r in router.routes]; assert '/recommended' in routes; assert '/search' in routes; print('dishes router OK')" && python -c "from routes.suggestion import router; routes = [r.path for r in router.routes]; assert '/today' in routes; print('suggestion router OK')"</automated>
  </verify>
  <acceptance_criteria>
    - routes/dishes.py exports router with GET /recommended and GET /search endpoints
    - GET /search accepts query parameter named "keyword" with default ""
    - Search uses or_ with .contains() on Dish.name, Dish.canteen, Dish.window, Dish.tags
    - Empty keyword returns [] without hitting the database
    - routes/suggestion.py exports router with GET /today endpoint
    - Suggestion endpoint has 5 hardcoded text+dish_id entries matching mockApi.ts exactly
    - Uses random.choice to select one suggestion per request
    - Queries Dish by selected dish_id and returns TodaySuggestionOut with full nested dish
  </acceptance_criteria>
  <done>Both routes/dishes.py and routes/suggestion.py importable; dishes router has /recommended + /search paths; suggestion router has /today path</done>
</task>

<task type="auto">
  <name>Task 3: Search route + wire all routers into main.py + end-to-end verification</name>
  <files>what_to_eat_today_web/backend/routes/search.py, what_to_eat_today_web/backend/main.py</files>
  <read_first>
    - what_to_eat_today_web/backend/main.py (current app setup, where to add include_router calls)
    - what_to_eat_today_web/frontend/src/mock/mockApi.ts (getHotKeywords lines 159-166, getSearchSuggestions lines 172-191)
    - .planning/phases/02-core-endpoints/02-CONTEXT.md (D-09, D-10 for hot-keywords logic)
  </read_first>
  <action>
    1. Create routes/search.py per D-07:
       - router = APIRouter(prefix="/api/search", tags=["search"])
       - GET /hot-keywords: response_model=list[str]
         Per D-09, D-10: query all Dish rows, parse each dish.tags JSON to list, flatten all tags, count frequency with collections.Counter, return top 10 by most_common
         Import json and collections at module level
       - GET /suggestions with Query parameter keyword (str, default=""):
         If keyword is empty/whitespace, return []
         Otherwise: query all Dish rows, collect matching strings from dish.name, dish.canteen, dish.window, and individual parsed tags where the value contains keyword
         Use a Python set to deduplicate, return as list sliced to max 8 items
         This mirrors mockApi.ts getSearchSuggestions behavior exactly

    2. In main.py, after the CORS middleware block and before the health_check route, add 4 router imports and include_router calls:
       - from routes.canteens import router as canteens_router
       - from routes.dishes import router as dishes_router
       - from routes.suggestion import router as suggestion_router
       - from routes.search import router as search_router
       - app.include_router(canteens_router)
       - app.include_router(dishes_router)
       - app.include_router(suggestion_router)
       - app.include_router(search_router)

    3. Start uvicorn and run curl verification against all 6 endpoints matching ROADMAP success criteria
  </action>
  <verify>
    <automated>cd what_to_eat_today_web/backend && python -c "from main import app; routes = [r.path for r in app.routes]; assert '/api/canteens' in routes; assert '/api/dishes/recommended' in routes; assert '/api/dishes/search' in routes; assert '/api/suggestion/today' in routes; assert '/api/search/hot-keywords' in routes; assert '/api/search/suggestions' in routes; print('All 6 routes registered')"</automated>
  </verify>
  <acceptance_criteria>
    - main.py imports all 4 routers with aliased names (canteens_router, dishes_router, suggestion_router, search_router) and calls app.include_router for each
    - routes/search.py exports router with GET /hot-keywords and GET /suggestions endpoints
    - hot-keywords endpoint: queries Dish table, parses tags JSON, uses collections.Counter, returns top 10 strings by frequency
    - suggestions endpoint: accepts keyword query param, returns max 8 deduplicated matching strings from dish name/canteen/window/tags fields; empty keyword returns []
    - All 6 API paths registered on app: /api/canteens, /api/dishes/recommended, /api/dishes/search, /api/suggestion/today, /api/search/hot-keywords, /api/search/suggestions
    - Server starts without import errors: uvicorn main:app --port 8000
  </acceptance_criteria>
  <done>All 6 endpoints registered on FastAPI app; server starts cleanly; curl to each endpoint returns expected JSON shape matching ROADMAP success criteria</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | User-provided keyword query parameter crosses into SQL query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | /api/dishes/search keyword param | mitigate | SQLAlchemy .contains() uses parameterized queries - no raw string interpolation |
| T-02-02 | Tampering | /api/search/suggestions keyword param | mitigate | Same parameterized query via SQLAlchemy ORM |
| T-02-03 | Information Disclosure | Error responses | mitigate | FastAPI default 500 handler does not expose stack traces in production mode |
| T-02-04 | Denial of Service | Unbounded query results | accept | Dataset is 7 dishes total - no pagination needed at this scale |
</threat_model>

<verification>
Start server and run all 5 ROADMAP success criteria curl commands:
1. curl http://localhost:8000/api/canteens -> 3 canteens with camelCase keys (id, name, openTime, status, distance)
2. curl http://localhost:8000/api/dishes/recommended -> 7 dishes with tags as string arrays
3. curl "http://localhost:8000/api/dishes/search?keyword=面" -> matching dishes; empty keyword -> []
4. curl http://localhost:8000/api/suggestion/today -> {text, highlightDish} with nested full Dish object
5. curl http://localhost:8000/api/search/hot-keywords -> array of strings (up to 10); curl "http://localhost:8000/api/search/suggestions?keyword=面" -> max 8 suggestions
</verification>

<success_criteria>
All 6 data API endpoints respond with correctly-shaped JSON matching frontend TypeScript interfaces. Server starts with no errors. camelCase field aliases confirmed in all responses.
</success_criteria>

<output>
Create `.planning/phases/02-core-endpoints/02-01-SUMMARY.md` when done
</output>
