## 使用方法

1. 在脚本所在的目录下，**有一个名为 `images` 的文件夹**
2. 把需要识别的 `.jpg` / `.jpeg` / `.png` 图片放到该文件夹里。  
3. 运行脚本。  
4. 程序结束后，会在脚本同级目录生成 `ocr_results.sql` 文件，其中包含：
   - `CREATE TABLE` 建表语句
   - 每张图识别出的每一行文字对应一条 `INSERT` 语句
